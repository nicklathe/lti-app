{
  "name": "lti-test-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker-run": "docker rm lti-test-app && docker run -p 3000:3000 --name lti-test-app lti-test-app",
    "docker-build": "docker build . -t lti-test-app"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.2.3",
    "express": "^4.18.2"
  }
}
